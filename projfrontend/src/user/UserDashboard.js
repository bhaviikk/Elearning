import React,{useEffect} from "react";
import Base from "../core/Base";

const UserDashboard = () => {
  return (
    <Base title="User dashboard" description="here is your journey">
      <h1>Welcome to user dashboard page</h1>
    </Base>
  );
};

export default UserDashboard;
